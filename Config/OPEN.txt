# ==========================================
# 开源构建 (Open-Source Variant)
# ==========================================
# WiFi 驱动: mt76 (开源)
# 内核: 6.x (mainline)
# 源码: immortalwrt/immortalwrt (openwrt-25.12 stable)
# 设备支持: 通过 patches/ 注入 DTS + filogic.mk
#
# 本文件为开源构建标识，实际配置由 GENERAL.txt + NRADIO-C8-660.txt 提供。

# --- 开源构建专用: Aurora 主题 ---
CONFIG_PACKAGE_luci-theme-aurora=y

# --- 开源构建专用: 分区扩容 ---
CONFIG_PACKAGE_luci-app-partexp=y

# --- 开源构建专用: 现代代理平台 (firewall4/nft only) ---
CONFIG_PACKAGE_luci-app-homeproxy=y
CONFIG_PACKAGE_luci-i18n-homeproxy-zh-cn=y
CONFIG_PACKAGE_sing-box=y
CONFIG_PACKAGE_ucode-mod-digest=y
