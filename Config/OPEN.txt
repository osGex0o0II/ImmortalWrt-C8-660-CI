# ==========================================
# 开源构建 (Open-Source Variant)
# ==========================================
# WiFi 驱动: mt76 (开源)
# 内核: 6.x (mainline)
# 源码: immortalwrt/immortalwrt (master rolling, mediatek 内核 6.18；可切回 openwrt-25.12 stable)
# 设备支持: 通过 patches/ 注入 DTS + filogic.mk
#
# 本文件提供开源构建专用配置，通用配置由 GENERAL.txt + NRADIO-C8-660.txt 提供。

# --- 开源构建专用: Aurora 主题 ---
CONFIG_PACKAGE_luci-theme-aurora=y

# --- 开源构建专用: HomeProxy / sing-box ---
CONFIG_PACKAGE_luci-app-homeproxy=y
CONFIG_PACKAGE_luci-i18n-homeproxy-zh-cn=y
CONFIG_PACKAGE_sing-box=y
CONFIG_PACKAGE_ucode-mod-digest=y

# --- 开源构建专用: WOL Ultra（增强型网络唤醒：计划任务/批量唤醒，依赖 etherwake） ---
CONFIG_PACKAGE_luci-app-wolultra=y

# 默认开源镜像保留 C8 蜂窝 CPE、代理基线、Web 终端、UPnP、WOL 和 ZeroTier。
# partexp 等维护能力可通过 Config/PRIVATE.txt 按需启用。
